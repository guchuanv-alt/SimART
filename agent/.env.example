# Copy to agent/.env and replace with a newly issued DeepSeek API key.
# Do not commit agent/.env or paste a live key into chat/output.
DEEPSEEK_API_KEY=replace_with_a_new_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
